Genera la ficha del personaje usando solo la informacion incluida a continuación.

BOOK_LANGUAGE:
{{BOOK_LANGUAGE}}

CHARACTER_JSON:
{{CHARACTER_JSON}}

Si CHARACTER_JSON incluye `source_snippets`, úsalos solo como apoyo para verificar detalles ya asociados al personaje. No copies fragmentos largos ni añadas información que no esté respaldada por los datos del personaje.

Recuerda que `main_name` debe ser una única forma de nombre, preferiblemente la más completa o específica. Si hay variantes concatenadas o alternativas, sepáralas mentalmente: elige una como `main_name` y coloca las demás en `aliases`, sin repetir el `main_name`.
